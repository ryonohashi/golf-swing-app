# ゴルフスイング比較アプリ

打った1球を自動で動画に残し、自分で選んだ2球を重ねて、何が違ったのかをその場で見つけるiOSアプリ。

## 現在のフェーズ

**Phase 0: 技術POC**

実打の判定（スイング動作＋インパクト音のAND）とリングバッファが練習場の実環境で成立するかを確かめる段階。
実機での検証を待つ間に、製品のUI（`docs/design/` のモックに沿ったSwiftUI）を並行して先に実装する（2026-09-23 決定）。
このフェーズで書くコードは実験用であり、製品コードとして残すことを前提にしない。

目的と合否基準は `docs/poc/` を参照。

## 絶対に守る制約

- **原因を断定しない。** 2D姿勢推定ではフェース角も打点も原理的に映らない。「フォームが違う＝ミス」とは限らない。このアプリが示せるのは頭の移動量・肩の回転・腰のスウェー・体の傾き・テンポといった身体の動きの差分のみ。UI文言でもコード内のコメントでも「原因を特定する」「ミスを検出する」という表現を使わない。
- **1球＝1クリップ。** セッション全体を1本の動画として保存しない。
- **実打は動作と音のANDで判定する。** 音だけ、映像だけで実打と判定しない（手動モードを除く）。
- **比較する2球はユーザーが選ぶ。** アプリが「この球とこの球を比べるべき」と自動判定しない。
- **v1は完全ローカル・完全無料。** ネットワーク通信を行わない。

## 作らないもの

以下はすべて検討したうえで意図的に落としている。実装中に再提案しない。

| 作らないもの | 理由 |
|---|---|
| セッション全編の連続録画＋後処理での切り出し | 30分で数GB規模になり、手に取った瞬間に数万〜十数万フレームの姿勢推定が始まって分単位で待たされる。「その場で見る」が成立しない |
| 異質な球の自動抽出／比較候補の自動提示 | 2D姿勢ではフェース角・打点が見えず、「姿勢が他と違う＝ミス」は成立しない。v1.1以降に「おすすめ」として再検討 |
| 端末の持ち上げ検知による撮影の自動中断・再開 | 作らなくても壊れない。持ち上げれば人が写らなくなるだけで、戻せばまた写る。必要なのは電池対策だけで、トリガ無しのタイムアウトで足りる |
| 骨格上の矢印ヒント | 中級者向けには不要。数値の差分表示で代替でき、誤ったアドバイスを断定するリスクも避けられる |
| アウトライン（輪郭線）表示 | 毎フレームの人物セグメンテーションが必要で高価。寒色／暖色のティント分けで同じ判別性が得られる |
| 環境光センサーによる配色の自動切替 | iOSに環境光センサーの公開APIがない。手動の屋外トグルで代替 |
| 骨格ランドマークによる自動位置合わせ | 主用途である同一セッション内比較では端末が動かないため不要 |
| CloudKit / StoreKit / 課金 | v1は端末内で完結させる |
| LLMによる診断 | v1.1以降 |
| 3D姿勢推定 | v1.1以降 |
| 素振りの保存 | インパクト音がなく、クリップを切り出す基準の時刻が決まらない |
| Apple Watch連携 | 動作＋音の判定で撮影が自動化されるためリモート操作の必要がない |
| 120fpsを前提にすること | まず60fpsで検証し、不足が確認できてから採用する |

## 暫定値

以下は確定値ではない。定数として書く場合は設定として1箇所にまとめ、すぐ差し替えられる形にする。

| 値 | 暫定 | 確定するPOC |
|---|---|---|
| クリップの切り出し範囲 | インパクト音の前5秒＋後3秒 | POC-02 |
| リングバッファ長 | 8秒 | POC-02 |
| チャンク長 | 未定（1〜2秒を想定） | POC-02 |
| フレームレート | 60fps | POC-03 |
| デバウンス幅 | 未定（0.5〜1秒を想定） | POC-01 |
| スイング候補とインパクト音を対応付ける時間窓 | 未定 | POC-01 |
| 動き検出の閾値・ROI | 未定 | POC-01 |
| 実打判定の誤保存率の合格ライン | 自分の実打数の10%以下 | POC-01 |

## 技術スタック

- Swift / SwiftUI
- SwiftData（ローカル永続化）
- AVAudioEngine（インパクト音の検知）
- AVCaptureVideoDataOutput ＋ フレーム差分 / optical flow（軽量なスイング動作の検出）
- AVFoundation / AVAssetWriter（リングバッファ撮影）
- AVMutableComposition（動画の重ね合わせ）
- Vision（姿勢推定、`VNDetectHumanBodyPoseRequest`）
- 最小対応 iOS 18 / 実機必須（シミュレータではカメラ・マイクが使えない）

## ドキュメントの地図

| ファイル | 内容 | 読むべき時 |
|---|---|---|
| `docs/product.md` | プロダクト定義と限界、文言のルール | UI文言を書く時 |
| `docs/scope.md` | v1 / v1.1以降の切り分け | 機能追加を検討する時 |
| `docs/spec-capture.md` | 撮影仕様 | 撮影まわりを触る時 |
| `docs/spec-compare.md` | 比較仕様 | 比較まわりを触る時 |
| `docs/spec-ui.md` | UI仕様 | 画面を作る時 |
| `docs/poc/` | POCの目的と合否基準 | Phase 0 |
| `docs/design/penpot/` | Penpotの画面モックの書き出し（画面の見た目の正） | 画面を作る時 |
| `app/README.md` | 製品アプリ（SwiftUI）の構成、モックで代替している部分、モックとの差異 | アプリのコードを触る時 |
| `poc/*/README.md` | 各POCの計測アプリのビルド・計測・解析手順 | Phase 0 |

## 開発順

```
Phase 0  技術POC        実打の判定（動作＋音） → リングバッファ → 1球クリップ保存
Phase 1  撮影UX         2m先に置く → 打つ → フラッシュ＋本数表示
Phase 2  比較           最新球 → 基準スイング → 1タップ比較
Phase 3  任意比較       スイング一覧 → 2球選択 → 4コマ → オーバーレイ
Phase 4  実環境テスト   実際の練習場で30〜60分使う
Phase 5  v1完成
```

Phase 0が通らなかった場合のみ、撮影方式そのものを再設計する。

## 仕様の正

実装開始以降、このリポジトリ内のMarkdownを仕様の正とする。企画段階の検討経緯は別途残っているが、実装の根拠にはしない。
